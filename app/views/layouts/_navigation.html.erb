
<ul class="nav ">
<a class="brand" href="/">Company Logo</a>
    <li class="divider-vertical"></li>


        <li><%= link_to('Orders', {:controller=>'home', :action=>'orders'})  %></li>
        <li class="divider-vertical"></li>
        <li><%= link_to('Products', {:controller=>'home', :action=>'products'})  %></li>
        <li class="divider-vertical"></li>
        <% if current_user.has_role? :admin then %>
        <li><%= link_to('Settings', {:controller=>'home', :action=>'settings'})  %></li>
        <li class="divider-vertical"></li>
        <% end %>
      <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.email%> <b class="caret"></b></a>
      <ul class="dropdown-menu">
        <li>
          <%= link_to(destroy_user_session_path, :method=>'delete') do %>
            Sign out
          <% end %>
        </li>                          
      </ul>
    </li>
        <li class="divider-vertical"></li>

  </ul>
