<div class="container" ng-app="groovepacks" ng-controller="appCtrl">
    <div class="row navigation-row">
      <header class="navbar">
        <div class="container">
          <div class="span2">
            <a ui-sref="home" class="brand"><img src="assets/logo.png"></a>
          </div>
          <div class="span05">
            <ul class="nav" id="mainMenu">
              <li ng-show="current_user.is_super_admin || current_user.access_orders"><a ui-sref="orders">Orders</a></li>
              <li ng-show="current_user.is_super_admin || current_user.access_products"><a ui-sref="products">Products</a></li>
              <li ng-show="current_user.is_super_admin || current_user.access_settings"><a href="#/settings">Settings</a></li>
              <li ng-show="current_user.is_super_admin || current_user.access_scanpack"><a ui-sref="scanpack">Scan and Pack</a></li>
            </ul>
            <ul class="nav" id="mainMenu">
              <li><a href="" role="button" data-toggle="modal" ng-click='import_all_orders()'>Import Orders</a></li>
            </ul>
          </div>
          <div class="span3 pull-right">
            <div class="row-fluid">
              <ul class="nav pull-right">
                <li><a href="#">Home</a></li>
                <li><a href="#">Help</a></li>
              </ul>
            </div>
            <div class="row-fluid">

              <ul class="nav pull-right">
                <li class="dropdown">
                  <a href="" class="dropdown-toggle" data-toggle="dropdown">{{current_user.username}}<b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <li>
                      <a href="/users/sign_out" data-method="delete" rel="nofollow">Sign Out</a>
                    </li>
                  </ul>
                </li>
                <li><a href="#">Profile</a></li>
              </ul>
            </div>
          </div>
        </div>
      </header>
      <div groov-common groov-import="import_all_orders"></div>
    </div>
      <div ui-view></div>
</div>
