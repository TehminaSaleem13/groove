<!DOCTYPE html>
<html>
<head>
</head>
<body style="line-height: 8px; margin-left: 3px; margin-top: 2px; margin-right: 2px; margin-bottom: 1px">
    <%if !@product.product_barcodes.first.nil? && !@product.product_barcodes.first.barcode.nil? %>
      <%unless @product.primary_sku.nil?%>
        <span style="font-size: 13px; margin-left: 10px;"><%= @product.primary_sku %></span> <br/>
      <%end%>
      <span style="font-size: 12px; margin-bottom: 10px; margin-left: 10px;"><%= @product.name%></span>
      <br/>
      <br/>
      <% barcode_string = generate_barcode(@product.primary_barcode)%>
      <center><%= pdf_image_tag(@product.primary_barcode+".png", {:width => 255, :height => 42}) %></center>
      
      <p style="text-align:center; font-size:8px; line-height: 0px;">*<%=barcode_string%>*</p>
    <%else%>
      <br/>
      <span style="font-size: 20px; margin-bottom: 10px; margin-left: 45px;">Barcode not available</span>
    <%end%>
</body>
</html>

 
